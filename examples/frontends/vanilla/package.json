{
  "name": "chat-wgpu-vanilla",
  "private": true,
  "type": "module",
  "scripts": {
    "build:wasm": "cd ../../.. && RUSTFLAGS=\"--cfg=web_sys_unstable_apis\" wasm-pack build --target web --out-dir examples/frontends/vanilla/pkg --release",
    "serve": "bun server.ts",
    "dev": "bun run build:wasm && bun server.ts"
  }
}
